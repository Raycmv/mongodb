
function encript(message){
   //this is note the real (encript) function, this is just a answer to protected it the real one.
    const number = '0123456789';
    let encrypted_text = message + number;
    
    return encrypted_text
}

module.exports = encript;
